using System;

namespace MultiPrecision {

    //public sealed partial class MultiPrecision<N> {

    //    private static MultiPrecision<N> SinCosCore(MultiPrecision<N> x) {

    //        MultiPrecision<N> xpi = Ldexp(Abs(x) * PI, -1);
    //        MultiPrecision<N> xpi_int = Round(xpi), xpi_frac = xpi - xpi_int;
    //        Int64 cycle = (Int64)xpi_int;
    //    }
    //}
}
